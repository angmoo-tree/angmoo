"""Runtime-owned LocalAppData migration adapters.

PostgreSQL-to-SQLite import support ended before the first public SQLite-only
release. The remaining migration package owns only product data-root layout
and generation lifecycle transitions.
"""
