{
  "fixture_id": "world_minimal_publish_ready",
  "contract_version": "p0-contract-v1",
  "required_phase": "P1",
  "input": {"required_definition": "complete", "optional_settings": []},
  "expected": {
    "outcome": "success",
    "code": "world_publish_ready",
    "writes": ["world", "world_membership"],
    "provider_call_count": 0,
    "notes": ["CORE quality tier", "optional settings do not block publish"]
  },
  "variants": ["missing_required_field", "length_or_enum_exceeded", "foreign_key_missing", "duplicate_idempotency", "deleted_source", "inactive_membership", "stale_contract_hash"]
}
